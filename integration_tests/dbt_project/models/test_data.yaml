version: 2

sources:
  - name: test_data
    schema: "{{ target.schema + ('_' + var('schema_name_suffix', '') if var('schema_name_suffix', '') else '') }}"
    tables:
      - name: metrics_seed1
      - name: metrics_seed2
      - name: metrics_seed3
