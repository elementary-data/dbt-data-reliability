version: 2

models:
  - name: metrics_table
    config:
      tags:
        - metrics

  - name: metrics_incremental
    config:
      tags:
        - metrics

  - name: metrics_view
    config:
      tags:
        - metrics

sources:
  - name: test_data
    schema: test_seeds
    tables:
      - name: first_metrics_table_seed
      - name: second_metrics_table_seed
      - name: dummy
